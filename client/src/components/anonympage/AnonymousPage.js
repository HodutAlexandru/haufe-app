import React from "react";

class AnonymousPage extends React.Component {
    render() {
        return(
            <div>This is the anonymous page. You don't need to be authenticated to see this page.</div>
        );
    }
}

export default AnonymousPage;